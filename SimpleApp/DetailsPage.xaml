<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimpleApp.DetailsPage">

    <ScrollView>
        <Frame Margin="20" Padding="15" CornerRadius="10" BackgroundColor="WhiteSmoke">
            <VerticalStackLayout>

                <!-- Title -->
                <Label x:Name="titleLabel"
                       FontSize="Title"
                       FontAttributes="Bold"
                       Margin="0,0,0,10"
                       HorizontalOptions="Center"
                       TextColor="DarkBlue" />

                <!-- Divider -->
                <BoxView HeightRequest="1"
                         BackgroundColor="Gray"
                         Margin="0,0,0,10" />
                
                <!-- Content -->
                <Label x:Name="contentLabel"
                       FontSize="Body"
                       TextColor="Gray" />

            </VerticalStackLayout>
        </Frame>
    </ScrollView>
</ContentPage>
